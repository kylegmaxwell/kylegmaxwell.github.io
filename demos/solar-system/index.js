
function main() {
    console.log("INDEX");
    Test.run();
    // galaxy = new SolarSystem();
}

// Graphics g
function paint(ctx)
{
 //    if (wipe)
    // {
 //        wipe=false;
 //        g.clearRect(0,0,getWidth(),getHeight());
    // }
    // galaxy.step(g);
}