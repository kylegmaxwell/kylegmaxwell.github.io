
function main() {
	console.log("INDEX");
	// galaxy = new SolarSystem();
	let p = new Point(1,2);
	console.log(p.toString());
}

// Graphics g
function paint(ctx)
{
 //    if (wipe)
	// {
 //        wipe=false;
 //        g.clearRect(0,0,getWidth(),getHeight());
	// }
    // galaxy.step(g);
}